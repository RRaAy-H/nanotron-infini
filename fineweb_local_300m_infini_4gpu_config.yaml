general:
  benchmark_csv_path: null
  consumed_train_samples: 80000
  ignore_sanity_checks: false
  project: llama_300m_infini_4gpu_fineweb
  run: 4gpu_fineweb_300m_experiment
  seed: 42
  step: 20000

# Infini-Attention Configuration - Updated to match reference specs
infini_attention:
  segment_length: 1024
  turn_on_memory: true
  balance_factor_lr: 0.01
  balance_act_type: hard_sigmoid
  balance_init_type: zeros
  logging: true
  logging_interval: 1000
  log_grad: false
  log_segment_acts: false
  balance_factor_weight_decay: 0.0

# Model Configuration (300M parameters with 12 layers)
model:
  ddp_bucket_cap_mb: 25
  dtype: bfloat16
  init_method:
    std: 0.03125  # 1/sqrt(1024)
  make_vocab_size_divisible_by: 1
  model_config:
    bos_token_id: 1
    eos_token_id: 2
    hidden_act: silu
    initializer_range: 0.02
    hidden_size: 1024
    intermediate_size: 4096
    num_hidden_layers: 12
    is_llama_config: true
    max_position_embeddings: 8192  # Updated for reduced context
    num_attention_heads: 8
    num_key_value_heads: 8
    pad_token_id: null
    pretraining_tp: 1
    rms_norm_eps: 1.0e-05
    rope_scaling: null
    tie_word_embeddings: false
    use_cache: true
    vocab_size: 49152

# Optimizer Configuration - Adjusted for larger model and batch size
optimizer:
  accumulate_grad_in_fp32: true
  adam_beta1: 0.9
  adam_beta2: 0.95
  adam_eps: 1.0e-08
  torch_adam_is_fused: false
  clip_grad: 1.0
  learning_rate_scheduler:
    learning_rate: 0.00006  # Reduced for larger model and long sequences
    lr_decay_starting_step: null
    lr_decay_steps: null
    lr_decay_style: cosine
    lr_warmup_steps: 500  
    lr_warmup_style: linear
    min_decay_lr: 0.000006
  weight_decay: 0.1
  zero_stage: 0

# Parallelism Configuration - 4x GPUs with data parallelism
parallelism:
  dp: 4  # Data parallel across 4 GPUs
  tp: 1  # No tensor parallelism
  expert_parallel_size: 1
  pp: 1  # No pipeline parallelism
  pp_engine: 1f1b
  tp_linear_async_communication: false
  tp_mode: ALL_REDUCE

# Tokenizer Configuration
tokenizer:
  tokenizer_max_length: null
  tokenizer_name_or_path: lvwerra/the-tokenizer-v1
  tokenizer_revision: null

# Training Tokens Configuration - Adjusted for long sequences
tokens:
  batch_accumulation_per_replica: 1
  limit_test_batches: 0
  limit_val_batches: 0
  micro_batch_size: 1  # Keep at 1 due to memory constraints with 8K sequences
  sequence_length: 8192  # Reduced from 32768 to avoid OOM
  train_steps: 30000  # Extended training for better convergence
  val_check_interval: -1

# Data Configuration for Local FineWeb Dataset
data_stages:
  - name: "4GPU FineWeb 300M Stage"
    start_training_step: 1
    data:
      dataset:
        hf_dataset_or_datasets: "parquet"
        hf_dataset_config_name: null
        hf_dataset_splits: "train"
        text_column_name: "text"
        dataset_processing_num_proc_per_process: 48
        dataset_overwrite_cache: false
        data_dir: "/data1/dataset/HuggingFaceFW/fineweb/"
      num_loading_workers: 20
      seed: 42

# Checkpoints Configuration
checkpoints:
  checkpoint_interval: 2000  
  checkpoints_path: ./checkpoints/fineweb_4gpu_300m_infini
  checkpoints_path_is_shared_file_system: false
  resume_checkpoint_path: ./checkpoints/fineweb_4gpu_300m_infini/20000
  save_initial_state: true

# Logging Configuration
logging:
  iteration_step_info_interval: 50  
  log_level: info
  log_level_replica: info

# Profiler (disabled for simplicity)
profiler: null